version: 2

project_name: awst

before:
  hooks:
    - go mod tidy

builds:
  - id: awst
    main: ./
    binary: awst
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/kedwards/awst/v3/cmd.version={{.Version}}

archives:
  - id: default
    formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - README.md
      - CHANGELOG.md

checksum:
  name_template: checksums.txt

release:
  # Attach the signed Firefox container extension. It versions independently of
  # awst and is committed pre-signed (task ext:sign), so the release just ships
  # the current artifact — no AMO signing in CI. Asset name: awst-containers.xpi.
  extra_files:
    - glob: ./extension/awst-containers.xpi

snapshot:
  version_template: "{{ incpatch .Version }}-snapshot"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"
